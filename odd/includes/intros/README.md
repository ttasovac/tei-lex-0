# Intros to various sections in the Specification

These intros are inserted by `fix-spec.xsl` as part of our build process. They are inserted at a specific point in the pipeline, after the ODD file has been compiled. The sections that they are being inserted into do not exist in the initial `lex-0.odd`

